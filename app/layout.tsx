import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'PromptGuardian - AI Security Firewall',
  description: 'Real-time AI firewall that detects, explains, and rewrites prompt injection and jailbreak attempts in Large Language Model (LLM) applications.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}